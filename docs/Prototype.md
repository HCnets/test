# 原型草图（文本线框）

## 1) 顶部栏 + 全局抽屉

```
┌───────────────────────────────────────┐
│ 23:59 的赛博抽屉      [包][任务][成就][设] │
├───────────────────────────────────────┤
│                                       │
│             主舞台（Hub/VN/解谜）       │
│                                       │
│   ←左边缘滑出背包        右边缘滑出任务→  │
│                                       │
│                         ↑底部滑出快捷栏  │
└───────────────────────────────────────┘
```

## 2) 右侧抽屉（分页）

```
┌───────────────右侧抽屉────────────────┐
│ 任务日志 / 成就面板 / 系统设置    [×] │
│ [任务] [成就] [设置]                   │
│---------------------------------------│
│ 列表内容区（可滚动）                   │
│ - 任务卡：未解锁/进行中/可领取/已完成   │
│ - 成就卡：进度/奖励/跳转               │
│ - 设置：性能/FPS/音量/数据看板          │
└───────────────────────────────────────┘
```

## 3) VN2 演出（微恐）

```
┌───────────────────────────────────────┐
│ 背景渐变（corridor）+ 暗角 + 轻噪点     │
│ 立绘（2.5D呼吸）                       │
│                                       │
│          [短气泡 bubble("...")]        │
│                                       │
│ 对话框：逐字 + 选项                     │
│ QTE：0.8s 倒计时条（失败不堵主线）       │
└───────────────────────────────────────┘
```

## 4) 解谜（镜面建筑）

```
┌───────────────────────────────────────┐
│ 走廊氛围 + 轻扫描线                     │
│ 节点/道路                              │
│ - 金色：可达，可点移动                  │
│ - 白色：不可达，点后弹面板              │
│                                       │
│        ┌──抽屉式面板──┐                │
│        │ 左旋 右旋 升降 │                │
│        │ 提示 退出 收起  │                │
│        └─────────────┘                │
└───────────────────────────────────────┘
```

